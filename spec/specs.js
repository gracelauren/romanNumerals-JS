describe("romanNumerals", function() {

  // it('will return the value of a number in roman numerals by adding the value of all the symbols', function() {
  //   expect(romanNumerals(2)).to.equal("II");
  // });

  it('will return 3000 as MMM', function() {
    expect(romanNumerals(3000)).to.equal("MMM");
  });



});
